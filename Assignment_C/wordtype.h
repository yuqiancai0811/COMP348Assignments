
#ifndef WORDTYPE_H
#define WORDTYPE_H

#include <stdbool.h>

// Function prototypes for checking word types
bool isAlpha(const char *word);
bool isAlphanum(const char *word);
bool isAll(const char *word);

#endif // WORDTYPE_H

